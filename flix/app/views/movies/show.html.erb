

<h1 class="display-4"><%=@movie.title %></h1>
<%=image_for(@movie) %>

<div class="btn-group my-3" role="group">
  <%=link_to "Write a Review", new_movie_review_path(@movie), class:"btn btn-primary mr-2" %>
  <%=link_to "Reviews", movie_reviews_path(@movie), class:"btn btn-primary mr-2" %>
  <%=link_to "Edit Movie", edit_movie_path(@movie), class:"btn btn-primary mr-2" %>
  <%=link_to "Delete Movie", movie_path, method: :delete, data:{confirm:"Are you sure?"}, class:"btn btn-primary mr-2" %>
</div>

<table class="table table-sm">
  <thead>
  </thead>
  <tbody>
  <tr>
    <th>Description</th>
    <td><%=@movie.description%> </td>
  </tr>
  <tr>
    <th>Released</th>
    <td><%= @movie.released_on%></td>
  </tr>
  <tr>
    <th>Rating</th>
    <td><%= @movie.rating %></td>
  </tr>
  <tr>
    <th>Director</th>
    <td><%= @movie.director %></td>
  </tr>
  <tr>
    <th>Duration</th>
    <td><%= @movie.duration %></td>
  </tr>
  <tr>
    <th>Total Gross</th>
    <td><%= format_total_gross(@movie) %></td>
  </tr>
  <tr>
    <th>Average Ratings</th>
    <td><%=format_average_stars(@movie)%></td>
  </tr>
  </tbody>
</table>


<%if @movie.recent_reviews.any? %>
  <h2>Recent Reviews</h2>
  <%@movie.recent_reviews.each do |review| %>
    <ul class="list-group list-group-item flex-column align-items-start">
      <div class="d-flex w-100 justify-content-between">
        <h5 class="mb-1"><%=review.name %></h5>
        <small># days ago</small>
      </div>
      <p class="mb-1"><%=review.comment %></p>
    </ul>
  <%end %>
  <%=link_to "Read All Reviews", movie_reviews_path(@movie),class:"btn btn-primary my-3" %>
<%end %>

<br>
<br>
<br>
<br>
<br>
<br>
<br>
<br>
<br>
<br>
<br>
<br>


<h1><%= @movie.title%></h1>
<%=image_for(@movie) %>


<p><%=@movie.description%></p>
<strong>Release Date: </strong><%= @movie.released_on %> (<%=time_ago_in_words(@movie.released_on) %> ago)<br>
<strong>Rating: </strong><%=@movie.rating %><br>
<strong>Total Gross: </strong><%=format_total_gross(@movie) %><br>
<strong>Cast: </strong><%=@movie.cast%><br>
<strong>Director: </strong><%=@movie.director%><br>
<strong>Duration: </strong><%=@movie.duration%><br>
<strong>Average Rating: </strong><%=format_average_stars(@movie)%><br>
<strong><%= link_to "All Movies", root_path %></strong> |
<%=link_to "Write a Review", new_movie_review_path(@movie) %> |
<%=link_to "Reviews", movie_reviews_path(@movie) %> |
<%=link_to "Edit Movie", edit_movie_path(@movie) %> |
<%=link_to "Delete Movie", movie_path, method: :delete, data:{confirm:"Are you sure?"} %>

<div class="media">
  <img class="d-flex mr-3" data-src="holder.js/64x64?theme=sky" alt="Generic placeholder image">
  <div class="media-body">
    <h5 class="mt-0">Media heading</h5>
    Cras sit amet nibh libero, in gravida nulla. Nulla vel metus scelerisque ante sollicitudin. Cras purus odio, vestibulum in vulputate at, tempus viverra turpis. Fusce condimentum nunc ac nisi vulputate fringilla. Donec lacinia congue felis in faucibus.
  </div>
</div>

<%if @movie.recent_reviews.any? %>
  <h3>Recent Reviews</h3>
  <%@movie.recent_reviews.each do |review| %>
    <li>From: <%=review.name %></li>
    <li><%=review.comment %></li>
    <br>
  <%end %>
  <%=link_to "Read All Reviews", movie_reviews_path(@movie) %>
<%end %>
