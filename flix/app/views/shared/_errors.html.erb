<% if object.errors.any? %>

  <div class="alert alert-danger">
    You have <%=pluralize(object.errors.count, 'error')%>:

    <ul>
      <%object.errors.full_messages.each do |message| %>
        <li><%= message%></li>
      <%end %>
    </ul>
  </div>


<%end %>